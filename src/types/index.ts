export type TestimonioCardType =
    {
        id: number,
        img: string,
        testimonio: string,
        nombreTestigo: string
    }

export type QuestionType = {
    id: number,
    question: string,
    response: string
}

export type DimensionType = {
    x: number,
    y: number
}